import { Typography } from '@mui/material';

export default function AboutPage() {
  return (
    <Typography variant="body1">
      This is a Create React App + Material UI starter project.
    </Typography>
  );
}
